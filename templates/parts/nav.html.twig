<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="background-color: black; color: white; padding: 10px; ">
            <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home')}}">Accueil</a>
            <a href="{{ path('app_articles_index') }}" class="navbar-brand">Articles</a>
            {# {{ dump(app.user) }} #}
            <ul class="navbar-nav ms-auto nb-2 nb-lg-0">
            
            {# Une fois que l'User veut supprimer son compte, il se deconnectera directement et son compte sera desactivé #}
            {% if (app.user) and app.user.active == true %}
            {# Si le User est connecté, il pourra voir Se deconnecter/MonCompte #}
                <li class="nav-item"><a class="nav-link" href="{{ path('app_logout')}}">Se deconnecter</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_profil_show')}}">Mon compte</a></li>
            {% else %}
            {# Quand le User est deconnecté il voit SeConnecter et S'enregistrer #}
                <li class="nav-item"><a class="nav-link" href="{{ path('app_login')}}">Se connecter</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_register')}}">S'enregistrer</a></li>
            {% endif %}

               
            </ul>
            </div>
        </div>
</nav>
